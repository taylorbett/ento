import React from 'react';

export class Shift extends React.PureComponent {
    render() {
        return (
            <div className="shift">

            </div>
        );
    }
}

export default Shift;